# Make-Money-AI

Make Money AI is a project that seeks to leverage AI tools for building a finance portfolio. This works by having the user input their desired portfolio type (ie. prioritizing certain sectors, international investing, risk tolerance etc.) then the AI agent constructs a portfolio optimized to their desires.

# Installation

To install the project first do

`pip install -r requirements.txt`

then do like

`pip install -e .`
